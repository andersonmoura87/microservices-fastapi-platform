target-version = "py311"
line-length = 100

[lint]
select = ["E", "F", "I", "UP", "B"]
# B008: FastAPI relies on `Depends(...)` as a default argument value.
ignore = ["B008"]

# Alembic migration scaffolding is generated; keep it out of the strict set.
exclude = ["**/alembic/versions/*"]
